# ЛР‑1. Решение СЛАУ итерационными методами (Jacobi, MPI)

## Постановка
Ищем `x` из `A x = b` для матрицы:
- `A[i,i] = 2N + 1`, `A[i,j] = 1` при `i ≠ j`.
- Выбираем `x_true` (случайный), считаем `b = A x_true`, затем «забываем» `x_true` и находим `x` итерационно.

## Метод
Метод Якоби:
x^{k+1}_i = (b_i − Σ_{j≠i} a_{ij} x^k_j) / a_{ii}
Останов: `||A x − b||_2 / ||b||_2 < eps` или `k >= maxit`.

## Реализации
- `fast` — использует структуру `A = J + 2N·I` → O(N) на итерацию.
- `dense` — полноценная плотная `N×N` → O(N²) на итерацию.

## Сборка
Для компиляции программы с **MPI**, выполните команду:

```bash
mpicxx -O3 -std=c++17 -o jacobi_mpi jacobi_mpi.cpp
## Прогон 1/2/4/8/16 процессов
Для запуска программы с различным количеством процессов (1, 2, 4, 8, 16) используйте команду:

# Пример для 1 процесса
mpiexec -np 1 ./jacobi_mpi

# Пример для 2 процессов
mpiexec -np 2 ./jacobi_mpi

# Пример для 4 процессов
mpiexec -np 4 ./jacobi_mpi

# Пример для 8 процессов
mpiexec -np 8 ./jacobi_mpi

# Пример для 16 процессов
mpiexec -np 16 ./jacobi_mpi



| Потоки | N         | Время, s | Ускорение S | Эффективность, % |
| ------ | --------- | -------- | ----------- | ---------------- |
| 1      | 1 000 000 | 63.0814  | 1.00        | 100.0            |
| 2      | 1 000 000 | 24.4609  | 2.58        | 129.0            |
| 4      | 1 000 000 | 11.067   | 5.70        | 142.5            |
| 8      | 1 000 000 | 5.52779  | 11.41       | 142.6            |
| 16     | 1 000 000 | 2.94453  | 21.41       | 134.4            |
